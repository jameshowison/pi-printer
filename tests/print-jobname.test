# Print with a custom job-name (set via -d jobname=VALUE)
# Used to test control-character sanitisation in log output.
# Usage: ipptool -tv -f FILE -d filetype=application/pdf -d "jobname=NAME" ipp://localhost:PORT/ipp/print print-jobname.test
{
	NAME "Print-Job with custom job-name"
	OPERATION Print-Job

	GROUP operation-attributes-tag
	ATTR charset attributes-charset utf-8
	ATTR language attributes-natural-language en
	ATTR uri printer-uri $uri
	ATTR name requesting-user-name $user
	ATTR mimeMediaType document-format $filetype

	GROUP job-attributes-tag
	ATTR integer copies 1
	ATTR keyword sides two-sided-long-edge
	ATTR name job-name $jobname

	FILE $filename

	STATUS successful-ok
	STATUS successful-ok-ignored-or-substituted-attributes

	EXPECT job-id
	EXPECT job-uri
}
