# DL envelope, one-sided — must NOT be substituted to Letter
# Usage: ipptool -tv -f FILE -d filetype=application/pdf ipp://localhost:PORT/ipp/print print-envelope-simplex.test
{
	NAME "Print-Job DL envelope one-sided"
	OPERATION Print-Job

	GROUP operation-attributes-tag
	ATTR charset attributes-charset utf-8
	ATTR language attributes-natural-language en
	ATTR uri printer-uri $uri
	ATTR name requesting-user-name $user
	ATTR mimeMediaType document-format $filetype

	GROUP job-attributes-tag
	ATTR integer copies 1
	ATTR keyword sides one-sided
	ATTR keyword media iso_dl_110x220mm

	FILE $filename

	STATUS successful-ok
	STATUS successful-ok-ignored-or-substituted-attributes

	EXPECT job-id
	EXPECT job-uri
}
